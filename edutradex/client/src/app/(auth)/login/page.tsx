import { LoginForm } from '@/components/auth/LoginForm';

export const metadata = {
  title: 'Sign In - OptigoBroker',
  description: 'Sign in to your OptigoBroker trading account',
};

export default function LoginPage() {
  return <LoginForm />;
}
